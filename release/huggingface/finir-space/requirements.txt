# FinIR runtime: the public PyPI release (this is what end users install). The Space
# executes against exactly the package users get from `pip install finir`.
finir==0.1.0

# FinIR-Intent (this workstream) is not on PyPI yet, so it is installed from GitHub
# (pip's VCS syntax, supported natively by Hugging Face Spaces). Pinned to the exact
# commit carrying the v0.1.0 release-readiness baseline for reproducibility.
#
# Once `finir-intent` is published to PyPI, replace this line with
# `finir-intent==0.1.0`.
git+https://github.com/Olyxee/finir.git@64f251a1c0860d3413dc5616d9acc036978eb0e9#subdirectory=finir_intent

gradio>=4.0
jsonschema>=4.21
