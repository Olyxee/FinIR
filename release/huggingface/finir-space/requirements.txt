# FinIR runtime: the public PyPI release (this is what end users install). The Space
# executes against exactly the package users get from `pip install finir`.
finir==0.1.0

# FinIR-Intent (this workstream) is not on PyPI yet, so it is installed from GitHub
# (pip's VCS syntax, supported natively by Hugging Face Spaces). Pinned to a commit
# for reproducibility.
#
# PUBLISH-TIME NOTE: bump the SHA below to the commit that lands the v0.1.0 readiness
# work (the parser + benchmark updates in this directory) before deploying the Space,
# so the demo reflects the latest baseline. Once `finir-intent` is published to PyPI,
# replace this whole line with `finir-intent==0.1.0`.
git+https://github.com/Olyxee/finir.git@8232480efdad488780c305fddbc544c77123311b#subdirectory=finir_intent

gradio>=4.0
jsonschema>=4.21
